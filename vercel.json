{
    "buildCommand": "cd client && yarn install",
    "outputDirectory": "client/src/app.js",
    "rewrites": [
        {
          "source": "/api/(.*)",
          "destination": "/api/server.js"
        },
        {"source": "/(.*)", "destination": "/"}
    ]

    
}